import { LigneCommandeArticle } from './ligne-commande-article';

describe('LigneCommandeArticle', () => {
  it('should create an instance', () => {
    expect(new LigneCommandeArticle()).toBeTruthy();
  });
});
